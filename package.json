{
  "name": "content-collector",
  "version": "0.0.1",
  "type": "commonjs",
  "main": "src/index.js",
  "scripts": {
    "test": "jest",
    "lint": "npx eslint srv --ext cjs --fix",
    "release": "npx standard-version --no-verify && git push --follow-tags origin main",
    "coverage": "npm run test -- --coverage",
    "format": "npx prettier -w src"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/VirtualOctopus/VirtualOctopusJS.git"
  },
  "engines": {
    "node": ">=18",
    "npm": ">=8"
  },
  "devDependencies": {
    "@sap/eslint-plugin-cds": "^2.6.3",
    "@types/jest": "^29.5.2",
    "@types/node": "^18.16.19",
    "eslint": "^8.44.0",
    "jest": "^29.5.0",
    "prettier": "^2.8.8"
  },
  "dependencies": {
    "@cap-js/sqlite": "^1.0.0",
    "@sap/cds": "^7.0.0",
    "express": "^4.18.2",
    "express-session": "^1.17.3"
  }
}
